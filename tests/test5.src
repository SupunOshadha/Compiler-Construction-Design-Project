integer x; 
integer a;
integer b;
float my_array[10]; 

x := 1 + 2 * (3 - 4); 
b := x + 10.5; 
a := (x == 5) or (b < 1.0); 

if (a <> 0) then { 
    write(x); 
} else { 
    while (x <= 100) { 
        read(a); 
        x := x + 1; 
    } 
} 

my_function(x, a + b, 123); 

return (x * 2 / a);
